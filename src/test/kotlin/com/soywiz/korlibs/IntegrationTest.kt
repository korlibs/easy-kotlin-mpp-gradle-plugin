package com.soywiz.korlibs

import org.gradle.testfixtures.*
import org.gradle.wrapper.*
import org.junit.*
import org.junit.rules.*

class IntegrationTest {
	/*
	val tempFolder = TemporaryFolder().apply { create() }
	@After fun deleteTempFolder() = tempFolder.delete()

	val project = ProjectBuilder.builder()
		.withProjectDir(tempFolder.root)
		.build()
		.also { println("INITIALIZED PROJECT AT ${it.rootDir}") }

	@Test
	fun testApplyNoExceptionThrown() {
		project.plugins.apply(KorlibsPlugin::class.java)
	}
	*/
}